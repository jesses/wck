package {		import Box2DAS.*;	import Box2DAS.Collision.*;	import Box2DAS.Collision.Shapes.*;	import Box2DAS.Common.*;	import Box2DAS.Dynamics.*;	import Box2DAS.Dynamics.Contacts.*;	import Box2DAS.Dynamics.Joints.*;	import misc.*;	import flash.events.*;		public class Memory extends Test {				public var bodies:Vector.<b2Body>;				public function Memory() {			super();						Main.txt.text = "Memory Test (Rapidly Creating / Destroying Body & Fixture)";			b2Def.circle.m_radius = 13 / scale;			b2Def.body.position.x = 300 / scale;			b2Def.body.position.y = 300 / scale;			b2Def.body.type = b2Body.b2_dynamicBody;			b2Def.fixture.shape = b2Def.circle;			b2Def.fixture.density = 1;		}				public override function EnterFrame():void {			var i:int = 0;			if(bodies) {				for(i = 0; i < bodies.length; ++i) {					bodies[i].destroy();				}			}			bodies = new Vector.<b2Body>();			for(i = 0; i < 100; ++i) {				var b:b2Body = b2Def.body.create(this);				b2Def.fixture.create(b);				bodies.push(b);			}			super.EnterFrame();		}			}}