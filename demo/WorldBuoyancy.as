package {	import wck.*;	import misc.*;	import flash.utils.*;		public class WorldBuoyancy extends wck.World {				var i:int;				public override function create():void {			super.create();			i = setInterval(addWave, 4000);		}				public override function destroy():void {			super.destroy();			clearInterval(i);		}				public function addWave():void {			var b:Buoyancy = getChildByName('bwaves') as Buoyancy;			var w:Wave = new Wave();			w.amplitude = -200;			w.wavelength = 200;			w.clampFunc = Util.sinEaseInOut;			w.velocity = 5;			w.left = -300;			w.right = 0;			b.water.addWave(w);		}	}}