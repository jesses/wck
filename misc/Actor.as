package extras {		import Box2DAS.*;	import Box2DAS.Collision.*;	import Box2DAS.Collision.Shapes.*;	import Box2DAS.Common.*;	import Box2DAS.Dynamics.*;	import Box2DAS.Dynamics.Contacts.*;	import Box2DAS.Dynamics.Joints.*;	import cmodule.Box2D.*;	import wck.*;	import shapes.*;	import misc.*;	import extras.*;	import flash.utils.*;	import flash.events.*;	import flash.display.*;	import flash.text.*;	import flash.geom.*;		public class Actor extends BodyShape {				//public var contacts:GroundFinder;				public override function create():void {			reportBeginContact = true;			reportEndContact = true;			super.create();			//contacts = new GroundFinder();			//contacts.listenTo(this);			listenWhileVisible(world, World.TIME_STEP, movement, false, -1);		}				public function movement(e:Event):void {			/*contacts.process(gravity);			contacts.clean();			if(contacts.contact.left) {			trace('-------------');			trace(contacts.dir.left);			trace(contacts.cl.length);			trace(contacts.contact.left.normal); } */		}	}}