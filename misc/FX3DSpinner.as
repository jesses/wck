package misc {	import misc.*;	import flash.display.*;	import flash.events.*;	import flash.utils.*;	import flash.geom.*;		public class FX3DSpinner extends FXContainer {						public function FX3DSpinner() {			for(var i:int = 0; i < numChildren; ++i) {				var m:Matrix3D = new Matrix3D();				var v:Vector3D = new Vector3D(Math.random(), Math.random(), Math.random());				v.normalize();				m.appendRotation(Math.random() * 180, v);				getChildAt(i).transform.matrix3D = m;			}		}	}}